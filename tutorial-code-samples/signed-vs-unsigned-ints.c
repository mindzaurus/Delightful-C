
#include <stdio.h>
#include <limits.h>

int main()
{
  long long int lli = LONG_MIN;
  signed long long int slli = LONG_MIN;

  printf("long long int %lld \n", lli);
  printf("signed long long int %lld \n", slli);
}
