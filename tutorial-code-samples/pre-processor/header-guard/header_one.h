// header is included only if header guard
// is not defined

#ifndef HEADER_ONE_H  // header guard

//define the header guard below
// once this is done this header won't
// be included again

#define HEADER_ONE_H  // header guard

typedef short int whole_number;

enum TEST
{
  ONCE,
  TWICE
};

#endif  // header guard
