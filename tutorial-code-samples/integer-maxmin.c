#include <stdio.h>
#include <limits.h>
#include <float.h>

int main()
{

  printf("Minimum value of long long int %ld \n", LONG_MIN);
  printf("Maximum value of long long int %ld \n\n", LONG_MAX);

  printf("Minimum value of long int %d\n", INT_MIN);
  printf("Maximum value of long int %d \n", INT_MAX);

}
