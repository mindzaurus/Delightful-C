
#include <stdio.h>

int main()
{
  
  printf("int takes %ld bytes \n", sizeof(int));
  printf("long int takes %ld bytes \n", sizeof(long int));
  printf("float takes %ld bytes \n", sizeof(float));
  printf("double takes %ld bytes \n", sizeof(double));
  
}
